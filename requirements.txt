yt-dlp>=2024.1.0
mutagen>=1.47.0
Pillow>=10.0.0
pywebview>=6.0

# Только для Windows — backend WebView2 для pywebview
pythonnet>=3.0.0; sys_platform == "win32"

# Для Linux
pygobject>=3.42.0; sys_platform == "linux"

# Для macOS
pyobjc-framework-Cocoa>=9.0.0; sys_platform == "darwin"
pyobjc-framework-WebKit>=9.0.0; sys_platform == "darwin"
