name: Build ShipTones for Windows

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        if (Test-Path requirements.txt) { pip install -r requirements.txt }
        
    - name: Build EXE
      run: |
        pyinstaller --onefile --windowed --name "ShipTones" --icon="assets/icon.ico" main.py
        # Если иконки нет, пробуем без неё
        if (-not (Test-Path "dist/ShipTones.exe")) {
          pyinstaller --onefile --windowed --name "ShipTones" main.py
        }
        
    - name: Create Draft Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        draft: true
        files: dist/ShipTones.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Upload Artifact (for manual runs)
      if: github.event_name == 'workflow_dispatch'
      uses: actions/upload-artifact@v4
      with:
        name: ShipTones-Windows
        path: dist/ShipTones.exe
        retention-days: 30
